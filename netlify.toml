[build]
  command = "rm -rf package-lock.json node_modules && npm install && npm install vite && npm run build"
  publish = "dist"

[build.environment]
  NODE_ENV = "development"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
